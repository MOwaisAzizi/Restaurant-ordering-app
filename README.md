# Restaurant Ordering App

A simple Flutter mobile application with SQLite local storage, Provider state
management, email/password authentication, restaurant listings, menus, cart, and
checkout.

## Run

```bash
flutter pub get
flutter run
```

The database is created automatically on first launch and seeded with sample
restaurants and menu items.
